---
# SOC Home Lab — Master Provisioning Playbook
# Usage (from ansible/ directory):
#   ansible-playbook playbooks/site.yml
# To run a single VM group:
#   ansible-playbook playbooks/wazuh-manager.yml
#   ansible-playbook playbooks/windows-victim.yml
#   ansible-playbook playbooks/kali-attacker.yml

- ansible.builtin.import_playbook: wazuh-manager.yml
- ansible.builtin.import_playbook: windows-victim.yml
- ansible.builtin.import_playbook: kali-attacker.yml
