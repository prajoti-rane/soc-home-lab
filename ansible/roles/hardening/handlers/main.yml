---
- name: Restart sshd
  ansible.builtin.service:
    name: "{{ hardening_ssh_service_name }}"
    state: restarted

- name: Restart fail2ban
  ansible.builtin.service:
    name: fail2ban
    state: restarted
